fix(search): 修复魔法棒改写后点搜索输入框回变回原始词的串台 bug

根因：navigatingFromInputRef 是字符串 ref，跨多次操作串台：
  1) 用户前一次操作（切语言/切分页）会在 ref 里写旧字符串
  2) 用户点魔法棒改写 queryInput 为英文扩展，没清这个 ref
  3) 用户点搜索，applyParams 调 router.replace → useEffect 被触发
     → navigatingFromInputRef 里是上一次操作的旧值
     → 旧值 !== expected（新 query）→ 走 else 分支
     → setQueryInput(expected) 强制回写，用户以为"被还原了"

修法双保险：
1) navigatingFromInputRef 字符串匹配 → 双 ref 自增 token 校验：
   navigatingTokenRef(自增序列号) + navigatingExpectedRef(字符串)
   每次 applyParams 生成新 token，useEffect 精确判断 exact match，
   杜绝跨多次操作的"历史残留 ref"被误判为本次导航的情况
2) runRewriteDirect（魔法棒回调）在 3 条路径全部先清双 ref：
   - 入口（魔法棒刚被点）
   - rewrite API 成功返回后
   - rewrite API 失败 catch 后
  保证魔法棒改写完成后再点搜索，不会带上任何"历史导航残留"

另外：navigatingRef 的分支只有当 navigatingExpected !== expected 时
才 setQueryInput(expected) setRwExplanation(null)
如果 navigatingExpected === expected 就 return，保证搜索按钮提交时
URL query 是魔法棒改写后的英文 → useEffect 命中相等分支，不做任何回写
